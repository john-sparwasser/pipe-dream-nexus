# pipe-dream-nexus

Static info site for [Pipe Dream](https://github.com/john-sparwasser/pipe-dream).

One `index.html`, no build step. Vercel serves it as-is.

Local preview: `npx serve .` (or just open the file).
